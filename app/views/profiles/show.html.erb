<div class="container">
  <div class="create-new-profile">
    <h1> <%= @profile.name %> </h1>
    <p> <%= @profile.essay1 %> </p>
    <p>Your link:  <%= link_to "/"+@profile.url, profile_path(@profile.url) %> </p><br>
  </div>
</div>
